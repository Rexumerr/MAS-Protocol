import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.multiversal.swarm',
  appName: 'Rexumer MAS',
  webDir: 'apps/web/dist'
};

export default config;
